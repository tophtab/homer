Load Homer project state and route the current request.

Run `python3 .homer/scripts/homer.py status`, then read `.homer/workflow.md`, `.homer/spec/hard-rules.md`, and `.homer/state/chapters.json`.

Use `homer-setup` for setup/import, `homer-write` for writing or polishing, and `homer-sync` for acceptance or knowledge rebuilds.
