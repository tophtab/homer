Write, continue, expand, polish, or revise a Homer draft chapter.

Follow the `homer-write` skill. Edit files directly under `正文/`, keep context minimal, and run `python3 .homer/scripts/homer.py check` after editing.

Do not auto-accept or sync after writing.
