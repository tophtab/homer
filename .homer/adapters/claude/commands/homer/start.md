Load Homer project state and route the current request.

Run:

```bash
python3 .homer/scripts/homer.py status
```

Then read `.homer/workflow.md`, `.homer/spec/hard-rules.md`, and `.homer/state/chapters.json`.

Route setup/import work to `homer-setup`, writing or polishing work to `homer-write`, and acceptance or knowledge rebuild work to `homer-sync`.
