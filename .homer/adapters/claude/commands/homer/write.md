Write, continue, expand, polish, or revise a Homer draft chapter.

Follow the `homer-write` skill. Edit files directly under `正文/`, keep context minimal, and run after editing:

```bash
python3 .homer/scripts/homer.py check
```

Do not auto-accept or sync after writing.
